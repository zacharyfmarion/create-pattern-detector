"""Synthetic crease-pattern dataset utilities.

The modules in this package are manifest-first: Bun/Rabbit Ear creates canonical
FOLD graphs, then Python rendering creates one manifest row per image variant.
Heavy training dependencies are imported lazily by the dataset/renderer modules.
"""

