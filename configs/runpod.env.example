# Copy to configs/runpod.env and fill locally.
# Do not commit real keys.

# Used by runpodctl and any RunPod automation scripts.
RUNPOD_API_KEY=

# Optional: fill after creating a pod if you want follow-up commands/scripts to
# target one pod without retyping the id.
RUNPOD_POD_ID=
